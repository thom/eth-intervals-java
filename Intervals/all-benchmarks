#!/bin/bash
MYDIR=$(dirname $0)
DIR=$(cd $MYDIR; pwd)
LOGDIR=${MYDIR}/measurements
LOGFILE=$(hostname)-$(date +'%Y-%m-%d-%H:%M:%S').txt
CLASSPATH=$CLASSPATH:$DIR/bin:$DIR/lib/* script -c "$MYDIR/all-benchmarks.1 $LOGFILE" $LOGDIR/$LOGFILE
